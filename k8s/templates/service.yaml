apiVersion: v1
kind: Service
metadata:
  name: users-api
  namespace: {{ .Values.global.namespace }}
spec:
  selector:
    app: users-api
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
